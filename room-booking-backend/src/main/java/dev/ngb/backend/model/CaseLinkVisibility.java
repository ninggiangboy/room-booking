package dev.ngb.backend.model;

/**
 * The visibility scope of {@code case_assertions}.
 */
public enum CaseLinkVisibility {

    /** Internal only. */
    INTERNAL_ONLY,

    /** Participant shared. */
    PARTICIPANT_SHARED,

    /** Restricted authority. */
    RESTRICTED_AUTHORITY
}
