package dev.ngb.backend.model;

/**
 * The remedy kind of {@code case_remedies}.
 */
public enum RemedyKind {

    /** Information. */
    INFORMATION,

    /** Apology. */
    APOLOGY,

    /** Operational assistance. */
    OPERATIONAL_ASSISTANCE,

    /** Host task. */
    HOST_TASK,

    /** Recleaning. */
    RECLEANING,

    /** Repair. */
    REPAIR,

    /** Replacement amenity. */
    REPLACEMENT_AMENITY,

    /** Access recovery. */
    ACCESS_RECOVERY,

    /** Booking cancellation. */
    BOOKING_CANCELLATION,

    /** Booking modification. */
    BOOKING_MODIFICATION,

    /** Relocation. */
    RELOCATION,

    /** Refund. */
    REFUND,

    /** Host waiver. */
    HOST_WAIVER,

    /** Travel credit. */
    TRAVEL_CREDIT,

    /** Coupon. */
    COUPON,

    /** Goodwill payment. */
    GOODWILL_PAYMENT,

    /** Receipted reimbursement. */
    RECEIPTED_REIMBURSEMENT,

    /** Claim payment. */
    CLAIM_PAYMENT,

    /** Dispute acceptance. */
    DISPUTE_ACCEPTANCE,

    /** Dispute representment. */
    DISPUTE_REPRESENTMENT,

    /** Payout hold. */
    PAYOUT_HOLD,

    /** Payout release. */
    PAYOUT_RELEASE,

    /** Recovery request. */
    RECOVERY_REQUEST,

    /** Listing correction. */
    LISTING_CORRECTION,

    /** Risk review request. */
    RISK_REVIEW_REQUEST,

    /** Moderation request. */
    MODERATION_REQUEST,

    /** Safety escalation. */
    SAFETY_ESCALATION
}
