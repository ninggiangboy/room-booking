package dev.ngb.backend.model;

/**
 * What an aspect profile describes.
 */
public enum AspectProfileSubjectType {
    /** One listing. */
    LISTING,
    /** One host. */
    HOST,
    /** One property. */
    PROPERTY
}
