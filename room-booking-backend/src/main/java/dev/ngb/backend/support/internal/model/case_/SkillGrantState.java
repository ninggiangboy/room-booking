package dev.ngb.backend.support.internal.model.case_;

/**
 * The state of {@code agent_skill_grants}.
 */
public enum SkillGrantState {

    /** Active. */
    ACTIVE,

    /** Suspended. */
    SUSPENDED,

    /** Expired. */
    EXPIRED,

    /** Revoked. */
    REVOKED
}
