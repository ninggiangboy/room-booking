package dev.ngb.backend.support.internal.model.case_;

/**
 * The participant role of {@code case_participants}.
 */
public enum CaseParticipantRole {

    /** Reporter. */
    REPORTER,

    /** Claimant. */
    CLAIMANT,

    /** Respondent. */
    RESPONDENT,

    /** Guest. */
    GUEST,

    /** Host. */
    HOST,

    /** Co host. */
    CO_HOST,

    /** Assigned agent. */
    ASSIGNED_AGENT,

    /** Supervisor. */
    SUPERVISOR,

    /** Specialist. */
    SPECIALIST,

    /** Adjuster. */
    ADJUSTER,

    /** Legal. */
    LEGAL,

    /** Interpreter. */
    INTERPRETER,

    /** Representative. */
    REPRESENTATIVE,

    /** Provider. */
    PROVIDER,

    /** Authority. */
    AUTHORITY,

    /** Observer. */
    OBSERVER
}
