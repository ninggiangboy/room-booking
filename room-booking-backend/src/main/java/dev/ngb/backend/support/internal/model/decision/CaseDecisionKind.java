package dev.ngb.backend.support.internal.model.decision;

/**
 * The decision kind of {@code case_decisions}.
 */
public enum CaseDecisionKind {

    /** Eligibility. */
    ELIGIBILITY,

    /** Liability. */
    LIABILITY,

    /** Valuation. */
    VALUATION,

    /** Remedy. */
    REMEDY,

    /** Coverage. */
    COVERAGE,

    /** Dispute strategy. */
    DISPUTE_STRATEGY,

    /** Appeal result. */
    APPEAL_RESULT,

    /** Closure. */
    CLOSURE,

    /** Recovery. */
    RECOVERY
}
