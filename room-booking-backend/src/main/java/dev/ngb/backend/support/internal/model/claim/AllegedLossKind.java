package dev.ngb.backend.support.internal.model.claim;

/**
 * The alleged loss kind of {@code damage_claim_items}.
 */
public enum AllegedLossKind {

    /** Damage. */
    DAMAGE,

    /** Theft. */
    THEFT,

    /** Loss. */
    LOSS,

    /** Soiling. */
    SOILING,

    /** Destruction. */
    DESTRUCTION,

    /** Excessive cleaning. */
    EXCESSIVE_CLEANING,

    /** Unauthorized use. */
    UNAUTHORIZED_USE
}
