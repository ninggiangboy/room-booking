package dev.ngb.backend.support.internal.model.case_;

/**
 * The channel of {@code case_contacts}.
 */
public enum CaseContactChannel {

    /** In app message. */
    IN_APP_MESSAGE,

    /** Email. */
    EMAIL,

    /** Sms. */
    SMS,

    /** Push. */
    PUSH,

    /** Phone. */
    PHONE,

    /** Chat. */
    CHAT,

    /** Postal. */
    POSTAL,

    /** Partner portal. */
    PARTNER_PORTAL,

    /** Provider portal. */
    PROVIDER_PORTAL
}
