package dev.ngb.backend.support.internal.model.remedy;

/**
 * The entry kind of {@code remedy_budget_consumptions}.
 */
public enum BudgetEntryKind {

    /** Reserve. */
    RESERVE,

    /** Consume. */
    CONSUME,

    /** Release. */
    RELEASE,

    /** Reverse. */
    REVERSE
}
