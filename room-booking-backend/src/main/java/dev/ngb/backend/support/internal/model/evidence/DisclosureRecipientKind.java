package dev.ngb.backend.support.internal.model.evidence;

/**
 * The recipient kind of {@code evidence_disclosure_manifests}.
 */
public enum DisclosureRecipientKind {

    /** Payment provider. */
    PAYMENT_PROVIDER,

    /** Insurer. */
    INSURER,

    /** Adjuster. */
    ADJUSTER,

    /** Participant. */
    PARTICIPANT,

    /** Regulator. */
    REGULATOR,

    /** Law enforcement. */
    LAW_ENFORCEMENT,

    /** Legal counsel. */
    LEGAL_COUNSEL
}
