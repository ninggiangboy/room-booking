package dev.ngb.backend.support.internal.model;

/**
 * The case type of {@code investigation_template_versions}.
 */
public enum SupportCaseType {

    /** Information request. */
    INFORMATION_REQUEST,

    /** Service complaint. */
    SERVICE_COMPLAINT,

    /** Stay problem. */
    STAY_PROBLEM,

    /** Safety report. */
    SAFETY_REPORT,

    /** Cancellation request. */
    CANCELLATION_REQUEST,

    /** Modification request. */
    MODIFICATION_REQUEST,

    /** Refund request. */
    REFUND_REQUEST,

    /** Payment problem. */
    PAYMENT_PROBLEM,

    /** Payout problem. */
    PAYOUT_PROBLEM,

    /** Damage claim. */
    DAMAGE_CLAIM,

    /** Provider dispute. */
    PROVIDER_DISPUTE,

    /** Protection claim. */
    PROTECTION_CLAIM,

    /** Account problem. */
    ACCOUNT_PROBLEM,

    /** Compliance request. */
    COMPLIANCE_REQUEST,

    /** Appeal. */
    APPEAL,

    /** Regulator complaint. */
    REGULATOR_COMPLAINT
}
