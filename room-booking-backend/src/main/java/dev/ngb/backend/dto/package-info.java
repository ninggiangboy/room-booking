/**
 * HTTP request and response data-transfer objects.
 *
 * <p>These DTOs are Java {@code record} types. A record generates a final field, accessor,
 * canonical constructor, {@code equals}, {@code hashCode}, and {@code toString} for every declared
 * component. Bean Validation annotations on request components reject invalid input before a
 * controller invokes its service.</p>
 */
package dev.ngb.backend.dto;
