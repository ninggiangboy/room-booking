package dev.ngb.backend.review.internal.model.aggregate;

/**
 * What a reputation purpose is computed about.
 */
public enum ReputationSubjectRole {
    /** A host. */
    HOST,
    /** A guest. */
    GUEST,
    /** A listing. */
    LISTING,
    /** A property. */
    PROPERTY
}
