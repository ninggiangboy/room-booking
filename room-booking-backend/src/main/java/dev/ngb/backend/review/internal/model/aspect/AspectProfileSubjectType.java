package dev.ngb.backend.review.internal.model.aspect;

/**
 * What an aspect profile describes.
 */
public enum AspectProfileSubjectType {
    /** One listing. */
    LISTING,
    /** One host. */
    HOST,
    /** One property. */
    PROPERTY
}
