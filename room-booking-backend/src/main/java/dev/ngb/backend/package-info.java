/**
 * Root package of the Room Booking Spring Boot application.
 *
 * <p>The {@code @SpringBootApplication} entry point lives here so Spring's default component
 * scan discovers every class in the child packages. Code is organized by technical role:
 * controllers receive HTTP requests, services implement use cases, repositories access the
 * database, and models represent persisted data.</p>
 */
package dev.ngb.backend;
